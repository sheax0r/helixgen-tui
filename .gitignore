__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
.pytest_cache/
.DS_Store

# paid commercial IR packs — never commit (user rule)
irs/

# WAV audio is never committed (paid-IR incident guard); force-add (-f) intentional synthesized fixtures
*.wav
*.WAV
